# HTTP Request Configuration
# This file contains the parameters for the concurrent request testing

# Target URL
URL="https://app.datarobot.com/api/v2/deployments/{deployment_id}/chat/completions"

# Headers (one per line, format: "Header-Name: Header-Value")
HEADER_1="Content-Type: application/json; charset=UTF-8"
HEADER_2="Authorization: Bearer {api_key}"

# JSON Data (as a single line)
DATA='{"model":"","messages":[{"role": "user", "content": "1000文字のホラーストーリーを作って"}],"temperature": 1}'
